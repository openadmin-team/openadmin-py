import { useSpec } from "./openadmin-spec"

export const openadminReference = ({ componentId }: {componentId: string}) => {
    const { data } = useSpec()
    // return compinentId, pageId and sectiondId
}