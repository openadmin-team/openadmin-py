name: "✍️ Spell Check"
description: "Check spelling with codespell"
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
    - shell: bash
      run: make check/spell
