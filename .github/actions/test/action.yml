name: "🧪 Test"
description: "Run pytest test suite"
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
    - shell: bash
      run: make check/test
