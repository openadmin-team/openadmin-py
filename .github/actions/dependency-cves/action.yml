name: "📦 Dependency CVEs"
description: "Run uv audit to check for known CVEs"
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
    - shell: bash
      run: make check/cves
